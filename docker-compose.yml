services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - IMGFLIP_USERNAME=${IMGFLIP_USERNAME:-}
      - IMGFLIP_PASSWORD=${IMGFLIP_PASSWORD:-}
    restart: unless-stopped

  # Optional: PostgreSQL for future DB sync
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: chaoshub
  #     POSTGRES_PASSWORD: chaoshub_secret
  #     POSTGRES_DB: chaoshub
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
